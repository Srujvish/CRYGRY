name: Cry Pattern Monitor

on:
  workflow_dispatch:
    inputs:
      run_minutes:
        description: 'Run duration (minutes)'
        required: false
        default: '30'

env:
  BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
  CHAT_ID: ${{ secrets.CHAT_ID }}

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install packages
      run: pip install requests pandas
        
    - name: Run with guaranteed alerts
      run: |
        echo "🚀 Starting GUARANTEED alert system..."
        echo "⏱️ Duration: ${{ github.event.inputs.run_minutes }} minutes"
        echo ""
        echo "📱 You WILL receive:"
        echo "1. ✅ Startup message immediately"
        echo "2. ✅ Price updates every 5 minutes per coin"
        echo "3. ✅ Movement alerts for >0.3% changes"
        echo "4. ✅ Heartbeats every 10 cycles"
        echo ""
        
        timeout $(( ${{ github.event.inputs.run_minutes }} * 60 )) python ultimate_algo.py
        
        echo "✅ Monitoring completed"
